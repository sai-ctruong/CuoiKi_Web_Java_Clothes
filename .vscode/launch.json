{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Debug (Attach) - Tomcat",
            "request": "attach",
            "hostName": "localhost",
            "port": 8000,
            "preLaunchTask": "Run on Tomcat (Embedded)"
        }
    ]
}
