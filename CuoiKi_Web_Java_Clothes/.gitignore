
# ----------------------------------------------------------------------
# 1. MAVEN (Quan trọng nhất)
# Loại bỏ thư mục chứa code đã biên dịch và file đóng gói (.war)
# ----------------------------------------------------------------------
/target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties

# ----------------------------------------------------------------------
# 2. NETBEANS IDE (Cấu hình riêng tư)
# Giữ lại nbproject/ nhưng bỏ thư mục private (chứa đường dẫn máy cá nhân)
# ----------------------------------------------------------------------
/nbproject/private/
/build/
/dist/
/nbbuild/
/.nb-gradle/

# ----------------------------------------------------------------------
# 3. JAVA Compiled Files (File nhị phân)
# ----------------------------------------------------------------------
*.class
*.log
*.ctxt

# ----------------------------------------------------------------------
# 4. PACKAGE FILES
# ----------------------------------------------------------------------
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# ----------------------------------------------------------------------
# 5. OS FILES (File rác của Mac/Windows)
# ----------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# ----------------------------------------------------------------------
# 6. LOCAL CONFIG (Tùy chọn)
# Nếu sau này nhóm chuyển sang dùng file properties để lưu pass DB
# thì bỏ dấu # ở dòng dưới để không lộ pass lên Git
# ----------------------------------------------------------------------
# src/main/resources/database.properties